# NaiveDistributedMLSystem
This is a naive system for distributed machine learning combined of a client, a master, a parameter server and a worker. 
Though simple and naive, it's qualified and it works.
